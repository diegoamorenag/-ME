name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  quality-checks:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          # CHANGE: Cache pnpm instead of npm
          cache: "pnpm"
          cache-dependency-path: "./frontend/pnpm-lock.yaml"

      - name: Install dependencies
        # CHANGE: Use pnpm instead of npm ci
        run: pnpm install --frozen-lockfile

      - name: Run linter
        # CHANGE: Use pnpm instead of npm/npx
        run: pnpm lint || pnpm eslint src --ext .js,.jsx,.ts,.tsx

      - name: Run tests
        # CHANGE: Use pnpm instead of npm
        run: pnpm test --watchAll=false --passWithNoTests
        env:
          CI: true

      - name: Build project
        # CHANGE: Use pnpm instead of npm
        run: pnpm build

      - name: Check build output
        run: |
          if [ ! -d "build" ]; then
            echo "Build directory not found!"
            exit 1
          fi
          echo "Build completed successfully!"
